var dodger = angular.module('dodge', []);
dodger.controller('dodgeCtrl', function ($scope) {
	$scope.message = "hello dodge";
});


//strucutre
//boxes(widgets) = pic + title     +    content(title + descr) 